export enum TicketStatus 
{
    OPEN = 'open',
    IN_PROGRESS = 'in_progress',
    WAITING_FOR_USER = 'waiting_for_user',
    RESOLVED = 'resolved',
    IN_REVIEW = 'in_review',
    CLOSED = 'closed',          
    REJECTED = 'rejected'
}
