export enum PaymentStatus 
{
    PENDING = 'pending',
    FAILED = 'failed',
    REFUNDED = 'refunded',
    SUCCESS = 'success',
}

export enum RefundStatus 
{
    NONE = 'none',
    PENDING = 'pending',
    SUCCESS = 'success',
    FAILED = 'failed'
}

